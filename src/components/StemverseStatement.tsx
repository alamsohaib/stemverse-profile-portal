import React from "react";

const StemverseStatement = () => (
  <section className="w-full flex flex-col items-center justify-center px-2 py-2 bg-[#f8f6ed] relative z-[5]">
    {/* Statement removed as it was redundant with the Join the STEM Movement section */}
  </section>
);

export default StemverseStatement;
